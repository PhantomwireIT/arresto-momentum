pub const DASHBOARD_UPDATE_MS: u64 = 500; // dashboard refresh interval
pub const CI_STEP_DELAY_MS: u64 = 700;    // CI/CD step delay
pub const PROGRESS_DELAY_MS: u64 = 50;    // progress bar speed
